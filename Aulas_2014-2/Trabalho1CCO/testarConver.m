%retorna 1 se o sistema tem convergência garantida e é recomendado usar o fator de relaxação
function r = testarConver( A )
	princ = A(1,1);
	n = columns(A)
	%varre as linhas
	for k = 1 : n
	% varre as colunas
		for 
			
		end
	end
end